{
  "name": "anas-chat-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p .",
    "start": "node dist/index.js",
    "migrate": "drizzle-kit migrate:latest --config drizzle.config.ts",
    "prepare": "npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "jsonwebtoken": "^9.0.0",
    "bcryptjs": "^2.4.3",
    "socket.io": "^4.7.2",
    "drizzle-orm": "^1.0.0",
    "drizzle-kit": "^1.0.0",
    "postgres": "^3.3.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "typescript": "^5.1.6",
    "ts-node-dev": "^2.0.0"
  }
}